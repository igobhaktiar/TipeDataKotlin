fun main() {
    // penggunaan tipe variable val, dan tipe data string
    // dengan melakukan inisialisasi nilai nya
    val name: String = "souya"

    // name = "Kelvin" // tidak bisa di lakukan init kembali

    // mencetak nilai menggunakan print dan println
    print(name)
    println(name)

    // penggunaan tipe variable var, dan tipe data string
    // tanpa melakukan inisialisasi nilai nya.
    var session: String
    // println(session) // tidak bisa di cetak karena belum ter init

    session = "CH1 TOP3"
    println(session)

    session = "CH1 TOP3 - Keren"
    println(session)
}